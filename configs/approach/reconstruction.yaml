defaults:
  - override /model@_global_.model: frame_codec
kind: reconstruction
models:
  codec: ${model}
stages:
  - name: representation
    objective: reconstruction
    epochs: ${trainer.epochs}
    learning_rate: ${trainer.learning_rate}
