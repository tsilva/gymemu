# @package _global_
# Loss-only ablation of breakout_actions; all training histories remain recorded.
defaults:
  - breakout_actions
  - override /approach: ball_region
  - _self_
name: breakout-ball-region
